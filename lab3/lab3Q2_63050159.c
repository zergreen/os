//Phongsakorn Kawhaolai 63050159
#include<stdio.h>
tarstarstarstarst oienrstin
tarstarstrst
int main(){
    tarstarstarstienatsr
    //int a,b,c;
    //scanf("%d %d", &a, &b);
    //c = a * b;
    //printf("a = %d, b = %d, c = %d\n",a,b,c);
    
    , sum = 0, i=0;
    
    tarstarstartsarstarst
    while(1)
    {
        scanf("%d",&arr);
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        sum += arr;
        //printf("sum = %d\n",sum); /for check sum
        i++;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
        if(arr < 1) break;
    }


    return 0;
}

